import React from 'react';
import {Contact, ProfileContacts} from './Contact';
import {ProfileResponseType} from '../../../redux/profile-reducer';

export const ProfileDataForm = ({profile}: { profile: ProfileResponseType }) => {
    return (
        <div>
            FORM
        </div>
    );
};